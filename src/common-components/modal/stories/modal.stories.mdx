import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { Modal, SaveButton} from 'common-components';
import { useState } from '@storybook/addons'

<Meta title="MDX | Modal" component={Modal} />

# Table Loader


```

import {TableNoResult} from "common-components"

```

### Default
<Preview>
    <Story name="Default">
      {
        () => {
          const [open, setOpen] = useState(false)
          return (
          <div>
            <SaveButton onClick={() => setOpen(true)}>Open Modal</SaveButton>
            <Modal open={open} title="Modal Test" onClose={() => setOpen(false)}>
              Modal Test
            </Modal>
          </div>
          );
        }
      }
    </Story>
</Preview>

