import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { DeleteModal, SaveButton as Button } from 'common-components'
import { useState } from '@storybook/addons'
const style={background: '#7c8a97'}

<Meta title="MDX|Delete Modal" component={DeleteModal} />

<Preview>
  <Story name="default">
    {() => {
      const [open, setOpen] = useState(false)
      const handleClose = () => {
        setOpen(false)
      }
      const handleDelete = () => {
        console.log('deleting')
      }
      return (
        <>
          <Button style={style} onClick={() => setOpen(true)}>Open Modal</Button>
          <DeleteModal
            open={open}
            header="Test delete modal"
            msg="group"
            name="Test group 101"
            closeFn={handleClose}
            delFn={handleDelete}
          />
        </>
      )
    }}
  </Story>
</Preview>

### Props

| Name    | Type     |          | Description       |
| ------- | -------- | -------- | ----------------- |
| open    | boolean  | required | to open the modal |
| header  | string   | -------- | header name       |
| msg     | string   | -------- | what type data    |
| name    | string   | required | name of data      |
| closeFn | function | required | close the modal   |
| delFn   | function | required | delete function   |

